<header>
  <!-- Fixed navbar -->
  <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
    <div class="container">
      <%= link_to("#{Rails.application.config.app_name}", root_path(), { class: "navbar-brand" }) %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              Components
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <%= link_to("Threat Sources", threat_sources_path(), { class: "dropdown-item" }) %>
              <%= link_to("Threat Events", threat_events_path(), { class: "dropdown-item" }) %>
              <div class="dropdown-divider"></div>
              <%= link_to("Vulnerabilities", vulnerabilities_path(), { class: "dropdown-item" }) %>
              <div class="dropdown-divider"></div>
              <%= link_to("Likelihoods", likelihoods_path(), { class: "dropdown-item" }) %>
              <div class="dropdown-divider"></div>
              <%= link_to("Impacts", impacts_path(), { class: "dropdown-item" }) %>
            </div>
          </li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <li class="nav-item <%= is_active?('static#about')%>">
            <%= link_to("About", about_path(), { class: "nav-link" }) %>
          </li>
        </ul>
      </div>
    </div>
  </nav>
</header>
