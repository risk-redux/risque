<header>
  <!-- Fixed navbar -->
  <nav class="navbar navbar-light" style="background-color: #ffffff;">
    <div class="container">
      <%= link_to(root_path(), { class: "navbar-brand", title: "risquè" }) do %>
      <%= "#{Rails.application.config.app_name}" %>
      <i class="fas fa-exclamation-triangle text-info"></i>
      <% end %>
    </div>
  </nav>
</header>